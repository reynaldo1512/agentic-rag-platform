# Datos

Carpeta reservada para documentos fuente (PDFs, textos, datasets) que serán
utilizados en próximas iteraciones como entrada del pipeline de ingesta del
Agentic RAG (carga, *chunking*, *embeddings* e indexación en el Vector Store).

En esta iteración de bootstrap está vacía intencionalmente.
